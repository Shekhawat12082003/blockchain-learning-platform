import MainLayout from './layouts/MainLayout';

// Re-export MainLayout as the default App to keep compatibility with any
// imports that still reference src/App. The Next pages use MainLayout directly.
export default MainLayout;
